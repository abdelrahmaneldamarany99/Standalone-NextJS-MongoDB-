import React from 'react'

const LoginRegisterLayout = ({children}:{children:React.ReactNode}) => {
  return (
    <div>
        <h1>Login & Register Layout</h1>
        {children}
    </div>
  )
}

export default LoginRegisterLayout